"use client";

export function AddToCartButton() {
  return (
    <button
      className={`mt-4 w-full py-3 font-label text-xs font-bold uppercase tracking-widest rounded-sm transition-all duration-300 hover-lift bg-primary text-on-primary`}
    >
      Add to Cart
    </button>
  );
}
